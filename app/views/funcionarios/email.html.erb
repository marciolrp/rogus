<h2>Enviar E-mail</h2>

<%= form_tag email_url do %>
<div class="control-group">
	<%= label_tag(:email, "E-mail:") %>
	<div class="controls">
		<%= text_field_tag(:email) %>
	</div>
</div>

<div class="form-actions">
	<%= submit_tag("Enviar") %>
</div>

<% end %>
